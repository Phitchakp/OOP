class Bankaccount {                                             // Class = {Bankaccount}
    double balance;                                             // Data = {balance}
    void deposit (double amount) {balance += aomunt; }          // Method = {deposit}
    double getBalance() {return balance}                        // Method = {getBalance}
}